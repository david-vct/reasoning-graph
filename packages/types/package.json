{
  "name": "@reasoning-graph/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'No build configured for types yet'",
    "type-check": "tsc --noEmit",
    "lint": "echo 'No linting configured for types yet'",
    "test": "echo 'No tests configured for types yet'"
  }
}
